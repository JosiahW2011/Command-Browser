Commands for your terminal:

`/search <query>` - Do a basic Google search.

`/bg <set|clear> <url>` - Set or clear the background image for both the search page with a source URL (Supports GIF, JPG, PNG, and IMG.)
